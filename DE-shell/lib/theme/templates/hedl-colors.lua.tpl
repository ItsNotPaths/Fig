-- hedl's four. ~/.config/hedl/colors.lua links here, hedl.lua requires it,
-- and a RELOAD after a theme change is what makes the borders follow.
return {
	focus  = "{{ accent }}",
	border = "{{ muted }}",
	urgent = "{{ red }}",
	root   = "{{ background }}",
}
